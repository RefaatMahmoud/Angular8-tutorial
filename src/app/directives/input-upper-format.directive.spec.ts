import { InputUpperFormatDirective } from './input-upper-format.directive';

describe('InputUpperFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new InputUpperFormatDirective();
    expect(directive).toBeTruthy();
  });
});
